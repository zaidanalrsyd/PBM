name: project_adaptive_response
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_adaptive_scaffold: ^0.1.6
  image_picker: ^0.8.5+3  # Image picker dependency for profile picture functionality

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/profile.jpg  # Ensure that this path exists if you are using local images
